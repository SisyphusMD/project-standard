# Per-project parameters for the vendored standard scripts. NOT vendored itself — each project
# owns this file, and it is deliberately outside the drift lock.
PROJECT_REPO_SLUG="SisyphusMD/<project>"
